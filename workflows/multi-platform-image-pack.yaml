workflow_id: multi-platform-image-pack
name: Multi Platform Image Pack (Legacy Compatibility Route)
version: 2.0.0
inherits: ecommerce-product-image-generation
compatibility_mode: compiler_default
platform_override: multi-platform
goal: Compatibility pointer for historical multi-platform runs. New runs compile the master contract plus platform overlays; do not load a copied step list.
route_defaults:
  requires_per_platform_overlay: true
