workflow_id: amazon-image-set
name: Amazon Image Set (Legacy Compatibility Route)
version: 2.0.0
inherits: ecommerce-product-image-generation
compatibility_mode: compiler_default
platform_override: amazon
goal: Compatibility pointer for historical Amazon listing runs. New runs compile the master contract plus the amazon platform override; do not load a copied step list.
route_defaults:
  image_count: 7
  require_clean_main_image: true
