workflow_id: tiktok-shop-image-set
name: TikTok Shop Image Set (Legacy Compatibility Route)
version: 2.0.0
inherits: ecommerce-product-image-generation
compatibility_mode: compiler_default
platform_override: tiktok-shop
goal: Compatibility pointer for historical TikTok Shop runs. New runs compile the master contract plus the tiktok-shop platform override; do not load a copied step list.
route_defaults:
  image_count: 5
  require_market_specific_overlay: true
