workflow_id: xiaohongshu-image-pack
name: Xiaohongshu Image Pack (Legacy Compatibility Route)
version: 2.0.0
inherits: ecommerce-product-image-generation
compatibility_mode: compiler_default
platform_override: xiaohongshu
goal: Compatibility pointer for historical Xiaohongshu runs. New runs compile the master contract plus the xiaohongshu platform override; do not load a copied step list.
route_defaults:
  image_count: 5
  content_native: true
