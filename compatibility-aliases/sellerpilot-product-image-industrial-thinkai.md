---
name: sellerpilot-product-image-industrial-thinkai
description: Migration-only compatibility entrypoint for SellerPilot Product Image. Prefer the single main skill and its automatic provider routing.
---

# SellerPilot Product Image ThinkAI Migration Alias

This is a migration template, not a normally installed skill. The supported user-facing entrypoint is `sellerpilot-product-image-industrial`.

When explicitly installed for a legacy user, load the main skill and resolve the provider with `--provider third_party_proxy`. Do not duplicate workflows, QA, canvas, or store-memory logic.
