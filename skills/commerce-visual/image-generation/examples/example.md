# Example for GPT Built-In Image Generation Execution

Output should be a personalized generation prompt with `provider: gpt-built-in-image-generation`, source image references when available, identity lock summary, allowed changes, forbidden changes, negative prompt, QA expectations, retry policy, and an execution boundary note that Codex should use the system `imagegen` skill / built-in `image_gen` tool when available. Add a request-pack block only when generation is unavailable or audit evidence is requested.
