# GPT Built-In Image Generation Execution

## Goal
Prepare personalized GPT built-in image generation prompts. In Codex chat/project contexts, execute real image generation through the system `imagegen` skill / built-in `image_gen` tool when available. Create request packs only as fallback or audit evidence when generation cannot execute or when the user asks for an audit package.

## Role in Loop
This Skill is part of the SellerPilot product image generation workflow.
